<h1>Editing friend</h1>

<%= render "form", friend: @friend %>

<br>

<div>
  <%= link_to "Show this friend", @friend, class:"btn btn-secondary"%>
  <%= button_to "Delete friend", @friend, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class:"btn btn-danger", method: :delete, form: {style: 'display:inline-block;'}%>
  <%= link_to "Back to friends", friends_path, class:"btn btn-secondary" %>
</div>
